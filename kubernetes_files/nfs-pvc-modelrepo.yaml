apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-model-repo
  namespace: bertapp
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs