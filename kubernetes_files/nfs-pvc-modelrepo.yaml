apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-model-repo
  namespace: bertapp
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
  storageClassName: nfs